#include <iostream>
#include <cstdlib>
using namespace std;
int eventhap = 1; 
int randnum = rand(25);
while loop{
switch (randomnum){
  case 0 && 1 && 2 && 3 && 4 && 5:
    #monsters will appear
  case 6 && 7:
    #hospital will appear
  case 8 && 9:
    #ammunation will appear
  case 10:
    #pawn shop will appear
  default:
    #keep walking
    #steps towards the kohinoor will increase by 1.
    #after 10-12 steps level change?
