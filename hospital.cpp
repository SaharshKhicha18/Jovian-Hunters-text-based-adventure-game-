#include <iostream>
using namespace std;

int hospital(int super_powers, int health_points)
{
  char health[10] = { 'healthkit', 'endorphins',  //add more items that increase health
  char superpower[10] = { 'ketamineshop', 'epinephrinshot', 'scapel', 'syringe', } // add more 
  int hp_increase[10] = {50, 32} //assignment of numeric health and superpower value
  int sp_increase[10] = {60, 40}
  for (int i = 0; i < 2; //size of the array 
  int random_option = rand() % 4;
  
}
